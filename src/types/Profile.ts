export type Profile = {
    userId: string;
    name: string;
    email: string;
    bio?: string;
    avatar?: string;
    $id?: string;
    $createdAt?: string;
    $updatedAt?: string;
    title: string;
}
